{
  "version": 2,
  "buildCommand": "cd ../.. && npx bun install --frozen-lockfile && cd packages/db && npx bun prisma generate && cd ../../apps/frontend/perpx-frontend && npx bun next build",
  "installCommand": "cd ../.. && npx bun install --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
